<%= render 'post', post: @post %>
<% if @post.user.id == current_user.id %>
    <div class="text-center edit-links">
      <%= link_to "Nazad", posts_path %>
      |
      <%= link_to "Izmeni Post", edit_post_path(@post) %>
    </div>
<% else %>
    <div class="text-center edit-links">
      <%= link_to "Nazad", posts_path %>
    </div>
<% end %>
